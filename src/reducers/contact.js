const initialContact = {
    testString: 'You can fond contacts hire.'
};

const contact = (state = initialContact, action = {}) => {
    switch (action.type) {
        // case 'SET_VISIBILITY_FILTER':
        //     return action.filter

        default:
            return state;
    }
};

export default contact;
